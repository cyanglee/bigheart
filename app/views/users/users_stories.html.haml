%h3 Users
.span8
  %table.table.table-condensed
    %thead
      %tr
        %th story name
        %th.col-md-2{:colspan => '2'} 編輯或刪除文章
        %th.col-md-1 current state
    %tbody
      - @stories.each do |story|
        %tr
          %td= link_to story.story_name, story_path(story.id)
          %td= link_to (button_tag '編輯文章', class: "btn btn-primary"), "#{edit_story_path(story.id)}"
          %td= link_to (button_tag '刪除文章', class: "btn btn-danger"), "#{story_path(story.id)}", confirm: '確定刪除?', method: :delete
          %td= story.human_state_name
